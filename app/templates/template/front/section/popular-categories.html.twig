{% if categories is not empty %}
    <section class="default-section popular-categories">
        <div class="container">
            <div class="wrap">
                <p class="title">{{ title ?? '' }}</p>

                {% include 'template/front/components/catalog-card.html.twig' with {
                    elements: categories,
                } %}
            </div>
        </div>
    </section>
{% endif %}
